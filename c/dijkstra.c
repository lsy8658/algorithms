#include <stdio.h>

int main () {
    /*
        다익스트라는 한 시작점에서 다른 모든 정점까지의 
        최단 거리를 구하는 알고리즘입니다.
        매번 가장 가까운 정점을 하나씩 확정해 나갑니다.
        단, 음수 가중치는 허용되지 않습니다.

        음수 가중치는 간선 비용이 0보다 작은 값이에요.
        예: A→B 비용이 -3이면 이동할수록 거리가 줄어듭니다.
        다익스트라는 “가까운 게 확정된다”는 
        전제가 깨져서 틀린 결과가 나옵니다.
    */
}